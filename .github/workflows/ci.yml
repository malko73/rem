name: CI

on:
  push:
    branches: [open, main]
  pull_request:
    branches: [open, main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        python -m pytest tests/ -v --tb=short

    - name: Legacy REM4 smoke test (reduced grid)
      run: |
        # This preserves the earlier REM4 numerical scaffold. It is not a
        # full rerun of the current Spec v2.2 Phase D / Phase E analyses.
        python src/rem4_numerical.py \
          --outdir outputs \
          --mode all \
          --state-mode ground \
          --j12 1.5 --j23 0.6 --h 0.2 \
          --lambda 0.2 \
          --lambda-max 1.0 \
          --grid-r 8 --grid-l 8
        ls -la outputs/
